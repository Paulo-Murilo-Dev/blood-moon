# Blood Moon for Waybar

Simple config based in catputcchin config 

## Installation

1. Copy `config.jsonc` and `style.css`to `~/.config/waybar/`.
2. change in the `config.jsonc` the Hyrpland for your WM.
3. Run `waybar`.